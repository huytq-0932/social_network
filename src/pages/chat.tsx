import React from "react";
import Document, { Head, Main, NextScript } from "next/document";

class Chat extends Document {
  render() {
    return <div>Chat day nha</div>;
  }
}

export default Chat;
